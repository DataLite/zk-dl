
<config>
    <!-- used to resolve dependency -->
    <config-name>ZkComposer</config-name>

    <!-- defines default behaviour of annotation processor -->
    <library-property>
        <name>zk-dl.annotation.cache</name>
        <value>true</value>
    </library-property>
</config>
